import { makeStyles } from "@material-ui/styles"

export default makeStyles(theme => ({
  paper: {
    marginBottom: "10px",
    width: "100%",
  },
  grid: {
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
  },
  container: {
    marginTop: "100px",
  },
}))
